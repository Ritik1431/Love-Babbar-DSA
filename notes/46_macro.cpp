#include <bits/stdc++.h>
using namespace std;

#define PI 3.14 // defining macro , also we cant change this (can't be override)

int main()
{
    int r = 5;
    int area = PI * r * r;
    cout<<area;

    return 0;
}